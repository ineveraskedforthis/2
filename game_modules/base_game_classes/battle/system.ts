// this module shoul require only characters and battles systems

var battles_list = []
var battles_dict = {}
var last_id = 0

export namespace BattleSystem {
    // only creates and initialise battle
    // does not add participants
    export function create_battle(){
        let battle = new Battle
    }
}